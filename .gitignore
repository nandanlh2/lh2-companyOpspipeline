# Secrets — API keys and Google OAuth material; never commit any of these
.env
client_secret*.json
.gmail_token.json

# Python
__pycache__/
*.pyc
.venv/

# Audit dumps are local run artifacts, keep them out of the repo
audit/*.json
